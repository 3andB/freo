# Use exactly one default server per listen address; replace any existing default.
server {
    listen 80 default_server;
    server_name _;
    return 404;
}
server {
    listen 443 ssl default_server;
    server_name _;
    ssl_reject_handshake on;
    return 404;
}
